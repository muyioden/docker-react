sudo: required
services:
  - docker

before_install:
  - docker build -t muyidocker/docker-react -f Dockerfile.dev .

script:
  - docker run muyidocker/docker-react npm run test -- --coverage

deploy:
  provider: elasticbeanstalk
  region: "us-west-2"
  app: "docker-react"
  env: "DockerReact-env"
  bucket_name: "elasticbeanstalk-us-west-2-139985006656"
  bucket_path: "docker-react"
  on:
    branch: master
